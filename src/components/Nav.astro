---
// Current page path
const url = new URL(Astro.request.url);
const path = url.pathname;
const page = path === "/" ? "home" : path.slice(1);
---
<nav class="text-sm lg:text-base flex space-x-8 mb-6">
  <a href="/" class={`inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium text-gray-900 dark:text-white ${page === 'home' ? 'border-amber-500' : 'border-transparent hover:border-amber-300 hover:text-gray-700 dark:hover:text-gray-300'}`}>Tilat</a>
  <a href="/grid" class={`inline-flex items-center border-b-2 px-1 pt-1 text-sm font-medium text-gray-900 dark:text-white ${page === 'grid' ? 'border-amber-500' : 'border-transparent hover:border-amber-300 hover:text-gray-700 dark:hover:text-gray-300'}`}>Pala keravaani ruudukot</a>
</nav>